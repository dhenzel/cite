name = "cite-mcp"
main = "src/index.ts"
compatibility_date = "2026-08-01"

# The bundled dataset (data/public-data.json) contains PUBLIC fields only:
# anonymized handles, scores, bands, prices, scrubbed summaries. No domains,
# no contacts, no seller prices. Generated by cite-mcp/src/export-public.ts.

[observability]
enabled = true
